echo "Building Node LTS Nix Container"
nix-build node.nix

if [ ! -z $USERNAME ]; then
	nix-shell --cores 0 --pure --keep USERNAME --keep PASSWORD -p skopeo --run "skopeo copy --dest-creds $USERNAME:$PASSWORD docker-archive://basal/result docker://sotekton/basal:nodejs"
fi
