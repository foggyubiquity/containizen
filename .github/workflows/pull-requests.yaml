name: pull requests to master
on:
  # check_suite:
  #   types: [ 'completed' ]
  pull_request:
    types: [opened, synchronize]
    branches:
      - master

jobs:
  call-oci:
    uses: ./.github/workflows/common-oci.yaml
    secrets: inherit

